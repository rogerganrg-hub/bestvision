steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["build", "-t", "${_IMAGE}", "-f", "apps/api/Dockerfile", "."]
images:
  - "${_IMAGE}"
substitutions:
  _IMAGE: "REGION-docker.pkg.dev/PROJECT_ID/bestvision/api:stg"
options:
  logging: CLOUD_LOGGING_ONLY
